{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-/Users/sbobba/projects/poc-sticker-map-site}\" || exit 0; [ -z \"$(git status --porcelain -- src astro.config.mjs wrangler.jsonc 2>/dev/null)\" ] && exit 0; npm run build > /tmp/sticker-map-build.log 2>&1 && exit 0; osascript -e 'display notification \"astro build is failing - see /tmp/sticker-map-build.log\" with title \"Sticker Map build\"' 2>/dev/null; echo '{\"systemMessage\":\"WARNING: astro build is failing - see /tmp/sticker-map-build.log\"}'",
            "async": true,
            "timeout": 120,
            "statusMessage": "Verifying build (astro build)..."
          }
        ]
      }
    ]
  }
}
